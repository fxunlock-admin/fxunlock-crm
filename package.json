{
  "name": "flowxchange",
  "version": "1.0.0",
  "private": true,
  "description": "Reverse-auction marketplace for FX affiliates and brokers",
  "scripts": {
    "dev": "npm --parallel -r dev",
    "build": "npm -r build",
    "test": "npm -r test",
    "lint": "npm -r lint",
    "db:migrate": "npm --filter backend db:migrate",
    "db:seed": "npm --filter backend db:seed",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
